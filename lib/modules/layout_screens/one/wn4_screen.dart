import 'package:flutter/material.dart';

class Wn4Screen extends StatelessWidget {
  const Wn4Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
          children: [
            Text('wn4'),
          ],
        ),
      ),
    );
  }
}
